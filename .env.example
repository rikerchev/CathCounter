# Base URL of the new Deno backend (server/). In production this is your
# deployed API's public URL; for local dev, the default matches
# `deno task dev` / `deno task start` in server/.
VITE_API_URL=http://localhost:8787
